datasource db {
  provider = "mysql"
  url      = "mysql://root:@localhost:3306/getzke"
}

generator client {
  provider = "prisma-client-js"
}

model Link {
  id          Int      @id @default(autoincrement())
  createdAt   DateTime @default(now())
  description String
  url         String
}
